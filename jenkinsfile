pipeline {
    agent any
    /* Slave resembles the server on which the job will be build on */
    stages {
         stage('Clone repository') {
             steps {
                 echo 'cloning'
         
       }
     }
   } 
 }
}
